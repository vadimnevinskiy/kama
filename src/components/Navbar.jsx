import React from 'react';

const Navbar = () => {
    return (
        <nav className='nav'>
            <a href="#">Profile</a>
            <a href="#">Messages</a>
            <a href="#">News</a>
            <a href="#">Music</a>
            <a href="#">Settings</a>
        </nav>
    );
}
export default Navbar;
